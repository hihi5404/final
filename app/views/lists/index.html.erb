<p id="notice"><%= notice %></p>

<h1>成績列表</h1>

<%= link_to '新增學生', new_list_path , class: "btn btn-success"%>

<hr>
<P>分數級別說明：90分以上S/80~89：A/70~79：B/60~69：C級/未滿60：D級</P>

<table class="table">
  <thead>
    <tr>
        <th>姓名</th>
      <th>學號</th>
      <th>分數</th>
      <th>處理</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= list.name %></td>
        <td><%= list.student_id %></td>
        <td><%= print_grade(list) %></td>
     <td><%= link_to '編輯', edit_list_path(list), class:"btn btn-success"  %></td>
        <td><%= link_to '刪除', list, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

